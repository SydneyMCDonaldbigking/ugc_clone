# 带货口播复刻流水线 — 项目交接文档

写给接手这个项目的下一个 agent。你没有前面的对话上下文,这份文档是自包含的。
读完应该能直接开工,不需要回头问人。

日期:2026-09-17
状态:**已跑通两条测试(umall_test、a2_test,均为 replica 模式)**,结构版量产未验证。进度见 `WORKFLOW.md`,做法见 `.claude/skills/ugc-voiceover-clone/SKILL.md`

---

## 一、这个项目要做什么

老板的原话需求:**"一模一样的口播(但是要改价格、商品啥的)来量产"**——
即拿别人跑通的带货视频,保留口播,换掉价格和商品,批量出片。

底层商业目标是明确且合理的:**复用一段已被市场验证能转化的口播结构,套到我们自己的
商品上批量生产**。

但需求原话里的"一模一样"这个口径**不能照字面执行**,原因见第二节。方案已经论证过,
结论是做**结构复用**而不是**逐字复用**。这不是合规上的妥协,是因为逐字复用在技术上
达不成"量产"这个目标本身。

---

## 二、最重要的一条:为什么逐字照搬口播反而量产不了

这是整个项目的设计前提,先看懂这条再动手。

### 2.1 它直接杀死量产目标

抖音 / TikTok 的查重不止做音频指纹,还会 **ASR 转文本后做文本查重**。同一段口播稿
只换商品名和价格批量发布,正是"洗稿/搬运"检测的**主要打击模式**,不是边缘风险。
量产的量越大,命中越快。结果是全线限流甚至封号,把整个矩阵账号带走。

**逐字照搬和"量产"这两个目标互相打架。** 结构复用才是唯一能规模化的路径。

### 2.2 广告法风险(罚的是甲方,不是平台处罚)

原口播里如果含效果宣称(例:"三天淡斑""一周瘦五斤"),那些宣称是针对**他们的商品**
做出的。原样搬到我们的商品上,就变成了对**我们商品**的虚假宣传。这是监管风险,
责任在广告主。

### 2.3 投诉门槛极低

逐字相同的文本,原作者截图一比对即可成立投诉,不需要复杂举证。

### 2.4 跟我们已有的家规冲突

仓库里已有一份写死的标准:

```
C:\Users\uryuu\Desktop\comfyui_workflow\.claude\skills\product-replication\SKILL.md
```

那份文档的核心是一张三栏表(**Copy this / Approximate this / Never inherit**),
其中 **Never inherit** 一栏明确包含:

- 他们的品牌、logo、片尾卡
- 他们的产品和所有变体
- **他们的口味语义、他们的宣传主张(their flavour semantics, their claims)**
- 他们的确切配色

逐字口播 = their claims 本身。新流水线必须沿用这条红线,不能开特例。

### 2.5 怎么跟老板沟通

实测有效的说法是讲业务后果而不是讲合规:
**"逐字照搬会被平台查重打掉,量产不了;抽结构才能真正批量出,而且转化力一样。"**

---

## 三、推荐方案:抽节拍表,不抽文案

带货口播的转化力不在那几个字,在**结构**。把参考视频的口播拆成一张**节拍表
(beat sheet)**,这是可合法复用、且真正可量产的资产。

### 3.1 抽什么 / 重生成什么

| 抽出来复用(结构) | 每条重新生成(内容) |
|---|---|
| 钩子类型(制造焦虑 / 反常识 / 身份认同)+ 占多少秒 | 具体台词文本 |
| 信息出现顺序(痛点 → 产品 → 卖点1/2/3 → 价格 → CTA) | 卖点内容(必须是我们商品真有的) |
| 每个节拍的词数、语速曲线 | 效果宣称(必须我们自己能背书) |
| 价格锚定手法(原价划掉 → 现价 → 限时) | 具体价格数字 |
| 语气强度曲线(哪里压低、哪里提速、哪里停顿) | 商品名、规格、CTA 话术 |
| 卖点与画面的对齐关系(说到 X 时切到 X 特写) | — |

产出的东西:**结构上跟爆款同构(保留转化力),文本上每条都不同(过查重),
宣称针对我们自己的商品(过广告法)**。

一张节拍表 + 一张商品参数表 = N 条不重样的口播。这才是量产。

### 3.2 建议的 skill 结构

要蒸馏的 skill 大致分五块,体例建议直接照抄 `product-replication/SKILL.md`
(保持跟现有家规一致):

1. **拆解阶段**
   用户提供参考视频 → 本地 `reference_archive.py` 建档并调用 faster-whisper 词级转写 → 完成 `ANALYSIS.md` / `TIMELINE.md` → 输出节拍表
   (每拍:时间窗、词数、功能标签、对应镜头与话画同步)

2. **节拍表 schema**
   定义清楚每一拍要记录什么字段。这是这个项目最核心的可复用资产,值得单独定版本。

3. **生成阶段**
   节拍表 + 商品参数表 → 新口播稿
   硬约束:结构对齐、文本不与原稿重合、宣称可背书

4. **红线清单**
   直接沿用 `product-replication/SKILL.md` 的 Never inherit 栏,再补三条:
   - 逐字文本
   - 效果宣称
   - 达人的脸和声音(换脸/克隆声涉及肖像权与人格权,风险等级完全不同,不碰)

5. **对齐回视频**
   新口播稿 → 词级时间戳 → 驱动现有 H3 分段生成流水线

---

## 四、hypit 框架评估结论(已调研,不用重做)

仓库:https://github.com/hypit-ai/hypit (TypeScript / Apache-2.0 with conditions / ~7.5k star)

它的自我定位就是"clone any viral video",功能上跟这个项目高度重合。已经评估过了,
**结论是不整体引入,只偷想法**。

### 4.1 它确实有的现成能力

- `yt-dlp` + `whisperx` 两个包:拆解参考视频、逐词对齐的链路是现成的
- **词锚定时间(word-anchored timing)**:时间绑在"词"上而不是"秒"上,改一句台词
  整条时间轴自动重排。这是它最好的想法,换 SKU / 价格 / CTA 时正好需要
- `caption` / `caption-fine` / `comment-sticker` / `screen-overlay` / `ranking` 等
  短视频组件(karaoke 字幕、评论贴纸、价格板)都是现成的
- **Model / Provider 分离**:Model 只声明请求语义,Provider/Endpoint 负责执行,
  凭证放 Runtime Profile。这层分得对
- 每种生成模式是独立 Surface(`TextVideo` / `FrameVideo` / `ReferenceVideo`)
  而不是一个 `mode=` 参数,让非法组合在写的时候就表达不出来
- 自带 `@hypit/minimax-h3` 包(跟我们用的模型是同一个)

### 4.2 为什么不整体引入

1. **形状相反**:我们的流水线是**参考图锚定 + 本地 GPU + 逐段强连续性锁 + 二采到
   1440x2560**;hypit 是**构图锚定 + 托管生成**(Seedance / GPT-Image 走 HypiHub)。
   要桥接得写一个 Provider endpoint 包住 `cc_submit.py`,但我们的 worker 是
   **文件队列 + 轮询**,不是请求/响应 API。这是周级别的集成项目,不是改配置。

2. **解决不了我们真正难的问题**:实际消耗重跑次数的都是生成层缺陷(锤肉时胡椒消失、
   筷子一次夹两块、月饼依次变色而非同步、10秒片4秒处断层、25秒爆内存)。
   这些全在**生成提示词层**,hypit 管的是"镜头怎么拼",一个都拦不住。

3. **~120 个包过度拆分**:`caption`/`caption-fine`/`caption-fine-studio`、
   `media-track`/`media-track-studio`…… 几乎每个概念拆 2-3 个独立版本的包,
   版本矩阵是实打实的维护负担。

4. **SVML 是 XML 里塞 `{}` 表达式的缝合语法**,而目标用户是编码 agent——agent 写
   JSON/TS 的可靠度远高于写自创标记语言。另外脚本里 `<D | Dee>`(读音覆盖)、
   `||`(分镜断点)、`@{}`(可嵌套区间标记)三套记号叠在一行,是 agent 写错了
   不报错、只渲染出怪东西的那类设计。

5. **讽刺的是"一比一"恰恰是 hypit 做得最差的**:它的 clone 产出的是 workflow,
   素材全部重新生成,本来就不是像素级复刻。

### 4.3 值得偷的三样

- **词锚定字幕时间**(如果新流水线做口播带货,这个单独实现一个,不用吞 120 个包)
- **Model / Provider 分离**(以后换模型不用重写 segments.json)
- **按模式分 Surface,而不是 profile 字符串运行时校验**

---

## 五、现有基础设施(可直接复用)

### 5.1 渲染流水线

**源码仓库**:https://github.com/SydneyMCDonaldbigking/comfyui_quick_workflow
(用 `main` 分支。另一个分支 `skill/h3-motion-transfer` 是未合并的开发分支)

注意:GitHub 上还有个 `SydneyMCDonaldbigking/MINIMAXH3_Autoworkflow`,
那是**另一个不相关的项目**(写 prompt / 菜谱脚本那套),别搞混。

**服务器部署**:租用 GPU 服务器上的 `/opt/MINIMAXH3_2PASS_Autoworkflow`
(IP / 端口每次续租都会变,当前地址只写在本机 `~/.ssh/config` 的 `doubleflow` 里,不进仓库;RTX 4090 24GB)。
本机 `~/.ssh/config` 里有 `doubleflow` 这个 host 别名,注意它需要
`KexAlgorithms curve25519-sha256,ecdh-sha2-nistp256`,不然握手失败。
**服务器重启后 authorized_keys 会被清掉,要用密码重装一次公钥。**

**启动服务**(重启后两个都要手动拉起,conda 不在非交互式 ssh 的 PATH 里,
必须用全路径):

```bash
# ComfyUI
cd /opt/ComfyUI && nohup /home/node/anaconda3/bin/conda run --no-capture-output \
  -n h3director python main.py --listen 127.0.0.1 --port 8188 > /root/comfyui_8188.log 2>&1 &

# worker
cd /opt/MINIMAXH3_2PASS_Autoworkflow && nohup /home/node/anaconda3/bin/conda run \
  --no-capture-output -n h3director python -m src.worker > logs/worker.log 2>&1 &
```

**主要脚本**:

| 脚本 | 用途 |
|---|---|
| `cc_submit.py` | 提交任务(`--segments-file` 多镜 / `--prompt-file` 单镜) |
| `cc_rerun.py` | 只重跑指定分镜,其余复用旧成片再重新合并 |
| `cc_status.py` | 查询 / `--wait` 等待 / `--deliver` 投递到 `/data/deliveries` |
| `cc_fetch.py` | 操作端拉回成片(sha256 校验) |
| `cc_upscale.py` | 把现成 MP4 走本地超分补到交付规格(用于跟外部 API 同标准对比) |

### 5.2 血泪教训(必读,这些都是烧过机时换来的)

1. **单个合并任务的安全上限是 20 秒 / 4 段。**
   25 秒 / 5 段连续炸了两次:ComfyUI 进程被 Linux OOM Killer 杀掉(占到 64GB,
   把 62GB 内存吃穿)。原因是**最后的 RealESRGAN 超分是对拼接后的整条视频做的**
   (每批 4 帧),每批都重新加载一次模型且没正确释放,泄漏量正比于**总帧数**。
   25 秒 = 600 帧 = 150 批,刚好越过临界点。
   **跟"是不是分段生成"无关,是总时长决定的。**
   要更长就拆成多个任务(20s + 15s 这种),各自独立渲染,最后自己拼。

2. **台词必须用精确标签,否则后端会强制注入"禁止任何人声"的最高优先级契约。**
   核心标签写法已查 `studio_node.py` 源码确认;新任务必须标为英文:

   ```
   (S1) <d>[English] English dialogue</d>
   ```

   正则只认 `<d>[Language]...</d>` 这个核心结构。`(S1)`/`(S2)` 是多说话人时的
   绑定标记。纯旁白/广告配音也用同样的标签(画面里没有嘴也能出声),在描述里
   写明是画外播音腔即可。历史 `[Chinese]` 测试证明了标签机制,但英文 H3 发音仍需先做短段实测。

3. **`cc_rerun.py` 的限制**:
   - 不能超出源任务原有的分镜总数(想加段只能重新提交完整任务)
   - `--prompt-file` / `--reference` / `--duration` **只在重跑单段时可用**
   - 多段重跑必须用 `--prompts-file`(JSON `{"2": "新词", "4": "新词"}`),
     且**不能改参考图**
   - 要同时改多段的参考图 → 拆成多次单段重跑,链式引用上一次的产物

4. **提示词格式**:四段式
   `subject_definitions` / `retention_analysis` / `detailed_description` / `soundscape`
   + 末尾一段禁止项约束。每段 5 秒为一个连续动作,段内不切镜。

5. **"参考图压过文字描述"**:参考图里有什么,模型就当作既成事实渲染什么。
   任何不想动的东西必须显式写"保持完全静止",否则会自己动起来。

6. **交付端限制**:超过 30MB 的文件无法推送到手机/网页端,只能在桌面端查看。

---

## 六、当前下一步(2026-09-20)

结构复用口径、本地 faster-whisper 转写、参考档案、schema、英文规划门禁和 Codex ImageGen 交接均已落地。
当前最小下一步是由 Claude 消费现有 `READY.json`,先渲染一个英文 H3 短段并做转写与人工画面验收;
通过后再跑英文整片和三变体批量验收。服务器渲染、输出 QA 和批处理还未进入统一 CLI。

---

## 七、边界提醒

这个项目会反复触碰"复刻别人内容"的边界,守住三条:

- **结构可以学,文本不能抄**
- **宣称必须是我们自己商品能背书的**
- **达人的脸和声音一律不碰**

前两条是业务能不能持续的问题,第三条是原则问题。
