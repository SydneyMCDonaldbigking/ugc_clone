# 带货口播结构复用流水线导学

> 基于 2026-09-19 工作区快照整理。本文只陈述仓库中可核验的实现和产物；个人职责、线上发布结果、转化指标均无证据，统一标为待补或待测。

## 1. 前置知识（面试高频标注）

| 知识点 | 为何需要 | 在本项目中的位置 | 高频度 |
| --- | --- | --- | --- |
| FFmpeg / FFprobe | 抽音轨、抽帧、探测媒体参数、检测镜头切点和制作静态保底镜头 | `scripts/prep.sh`、`scripts/tile.py`、`scripts/packshot_clip.sh` | 高 |
| 自动语音识别与词级时间戳 | 把参考口播从连续音频转换为可计算的文字、词时间窗和语速 | `scripts/transcribe.py`、`work/*/words.json` | 高 |
| 结构化内容建模 | 将“爆款视频”拆成节拍、功能、镜头、证据和文本等可组合数据 | `work/*/labels.json`、`work/*/beats.json`、`inputs/*/product.json` | 高 |
| 文本相似度与规则校验 | 限制原稿复用、控制字数预算、阻断禁用词和无证据宣称 | `scripts/check_script.py` | 高 |
| 生成式视频的参考图控制 | 理解身份保持、属性迁移、参考图编号、提示词互斥约束 | `work/*/segments.json`、`.claude/skills/ugc-voiceover-clone/SKILL.md` | 高 |
| 异步任务与局部重跑 | 生成任务耗时长、成本高，需要按分镜失败隔离与复用旧结果 | `work/a2_test/chain_reruns.sh`、`out/*/report*.json` | 中高 |
| 合规与证据链 | 商品宣称必须有包装、详情页或运营数据支撑，不能继承原片人物、声音和主张 | `HANDOFF.md`、`inputs/*/product.json` | 高 |
| 机器验收与人工验收边界 | 技术完整不等于内容正确，人物、商品、文字和剧情仍需人工复核 | `out/*/report*.json`、`WORKFLOW.md` S8 | 高 |

## 2. 重点亮点与学习顺序（先看这个）

| 亮点标题 | 为什么重要 | 通用技术关键词 | 先看哪些文件 | 建议学习顺序 |
| --- | --- | --- | --- | --- |
| 结构与内容解耦 | 项目不是逐字搬运，而是复用节奏、信息顺序和镜头关系，再用新商品事实生成新内容 | 领域建模、策略分离、合规边界 | `HANDOFF.md`、`WORKFLOW.md` | 1 |
| 词级时间驱动的节拍建模 | 把口播时长从“凭感觉写稿”变成可计算的字数、语速、停顿和镜头边界 | 时间轴、ASR、数据管道 | `scripts/transcribe.py`、`scripts/build_beats.py`、`work/umall_test/beats.json` | 2 |
| 商品事实证据约束 | 每条卖点通过事实 ID 追到证据，减少把原视频宣称错误迁移到新商品的风险 | 数据契约、可追溯性、规则引擎 | `inputs/umall_test/product.json`、`scripts/check_script.py` | 3 |
| 分段生成与局部恢复 | 20 秒视频拆为四个 5 秒段；失败时只重跑问题段，控制 GPU 成本和等待时间 | 任务编排、失败隔离、增量计算 | `work/a2_test/segments.json`、`work/a2_test/chain_reruns.sh`、`out/a2_test/report_004.json` | 4 |
| 双层质量门禁 | 自动检查分辨率、时长、音频、黑帧和冻结；语义、商品与人物一致性留给人工 | 自动化 QA、人工复核、责任边界 | `out/umall_test/report.json`、`.claude/skills/ugc-voiceover-clone/SKILL.md` | 5 |
| 原型经验沉淀为操作规范 | 两个样例中的 ASR 幻觉、密集小字、提示词自相矛盾和参考图冲突被写回 skill | 运行手册、知识沉淀、迭代学习 | `.claude/skills/ugc-voiceover-clone/SKILL.md` | 6 |

## 3. 必备知识点

- [ ] 能解释为什么项目复用“钩子类型、信息顺序、语速和画面对齐”，但不复用原文、原达人和原宣称。
- [ ] 能从 `ref_video.mp4` 讲到 `words.json`、`beats.json`、`script.json`、`segments.json` 和最终 MP4。
- [ ] 能说明 `product.json` 中 `facts[].id` 与 `script.json` 中 `claims[]` 的追溯关系。
- [ ] 能区分 `structure` 与 `replica`：前者是目标生产模式，后者只是贴原句式的测试模式。
- [ ] 能解释 5-gram、最长公共子串、字数预算和禁用词分别防什么问题。
- [ ] 能说明为什么单任务限制为 20 秒 / 4 段，以及局部重跑怎样减少重复计算。
- [ ] 能说明参考图的 `identity_preserved`、`attribute_transfer`、`fully_preserved` 各适合什么场景。
- [ ] 能指出机器预审只保证技术完整，报告状态仍是 `awaiting_human_review`。
- [ ] 能指出当前并未证明“量产”：现有成片全部是 `replica`，只有酸奶保存了一份未渲染的结构版脚本。

## 4. 推荐阅读（结合仓库）

| 主题 | 通用技术点 | 建议阅读位置 | 预计时间 | 读完能回答什么 |
| --- | --- | --- | --- | --- |
| 项目动机与边界 | 目标拆解、合规、版权与平台查重 | `HANDOFF.md` 第 1-3、7 节 | 15 分钟 | 为什么不能把“一模一样”按字面实现？ |
| 全链路设计 | 分阶段流水线、输入输出与通过条件 | `WORKFLOW.md` 第 0-3 节 | 25 分钟 | 八个阶段怎样串起来，哪个阶段依赖人？ |
| 参考视频预处理 | 媒体探测、抽音频、抽帧、场景检测 | `scripts/prep.sh` | 10 分钟 | 原视频如何变成后续分析所需的中间产物？ |
| 词级转写 | GPU 推理、逐词时间戳、中文提示和 VAD | `scripts/transcribe.py` | 20 分钟 | 为什么没使用 WhisperX，怎样处理背景音乐幻觉？ |
| 节拍表生成 | 时间窗聚合、字数/语速/停顿计算、人工标注融合 | `scripts/build_beats.py`、`work/umall_test/labels.json`、`work/umall_test/beats.json` | 30 分钟 | 一个节拍有哪些字段，哪些来自机器，哪些来自人工？ |
| 商品事实建模 | 证据引用、禁止宣称、未知值处理 | `inputs/umall_test/product.json`、`inputs/a2_test/product.json` | 15 分钟 | 一条商品口播事实怎样证明不是编造？ |
| 文案门禁 | 结构覆盖、字符预算、相似度、禁用词、事实引用 | `scripts/check_script.py`、`work/umall_test/script_structure_v1.json` | 25 分钟 | 结构版稿件怎样被规则化验收，当前校验器漏了什么？ |
| H3 提示词设计 | 参考图职责、连续动作、负面约束、声音标签 | `work/a2_test/segments.json` | 25 分钟 | 为什么提示词要分四段，参考图冲突如何导致穿帮？ |
| 局部重跑 | 长任务恢复、结果复用、成本控制 | `work/a2_test/chain_reruns.sh`、`work/a2_test/rerun_34.json` | 15 分钟 | 为什么 A2 能只重跑一个或两个分镜？ |
| 技术验收 | 编解码、分辨率、时长、黑帧、冻结、哈希 | `out/umall_test/report.json`、`out/a2_test/report_004.json` | 10 分钟 | `qa.passed=true` 为什么仍不能视为最终验收？ |
| 经验沉淀 | 从一次性脚本到可复用操作规范 | `.claude/skills/ugc-voiceover-clone/SKILL.md` | 30 分钟 | 两个样例暴露了哪些稳定的生成模型工程规律？ |

## 5. 自学提醒

建议阅读时始终用同一条主线核对：`参考视频 → 词级时间 → 节拍结构 → 商品证据 → 新口播 → 分镜提示词 → GPU 渲染 → 自动/人工验收`。不要先陷入某段英文提示词的细节，否则容易失去全局。

## 6. 项目技术定位

**AI 视频生成 + 内容工程 + 媒体处理的交叉型工程原型。** 依据是：本地通过 FFmpeg、JSON 数据契约和 Python/Bash 脚本完成拆解与编排，远端通过 faster-whisper 和 MiniMax H3/ComfyUI 完成 GPU 推理，同时把商品宣称证据和平台内容风险纳入生成约束。

它不是传统 Web 服务，也不是一个可独立安装运行的完整产品。当前仓库更像“操作台 + 中间数据 + 实验记录 + 运行手册”，真正的 H3 调度器位于外部仓库和租用 GPU 服务器。

## 7. 核心原理解析

### 7.1 从逐字复刻改为结构复用

**问题：** 原视频原文、人物、声音和宣称直接迁移，会带来文本查重、虚假宣传、版权与人格权风险，而且无法持续批量发布。

**机制：** 项目只提取钩子类型、功能顺序、节拍时长、语速、停顿、镜头类型和话画同步关系；商品名、价格、卖点和具体句子由新商品事实重新生成。

**项目落点：** `HANDOFF.md` 定义 Copy / Never inherit 边界，`beats.json` 保存结构，`product.json` 保存新商品事实，`script.json` 将两者组合。

### 7.2 用词级时间戳建立可计算的内容时间轴

**问题：** 仅看字幕或每秒一帧会漏字，也无法精确估计新口播能否在目标时长内说完。

**机制：** faster-whisper 输出每个词的起止时间和置信度；人工给节拍时间窗与功能标签，脚本再聚合出字数、每秒字数、停顿和内部切点。

**项目落点：** `transcribe.py` 产出 `words.json`，`build_beats.py` 将 `words.json + cuts.json + labels.json` 合并为 `beats.json`。

### 7.3 用证据引用约束生成式文案

**问题：** 生成模型容易把原片卖点、常识推断或营销话术当成新商品事实，尤其价格、配料和功效一旦错误会直接形成广告风险。

**机制：** 商品参数表把每条事实编号并附证据；文案每行通过 `claims[]` 引用事实 ID，校验器至少保证引用 ID 存在，并用禁用词拦截部分高风险表达。

**项目落点：** `inputs/*/product.json`、`scripts/check_script.py`。当前只验证“ID 存在”，还没有自动验证文本语义是否真的被证据覆盖，这是下一步缺口。

### 7.4 用分镜隔离生成失败

**问题：** 生成视频成本高、耗时长，单个动作或标签失败时重跑整条浪费算力；过长视频在超分阶段还会触发内存问题。

**机制：** 把视频装箱成四个 5 秒连续动作段，单任务限制在 20 秒；每段拥有独立提示词和参考图，问题段可基于旧任务结果单独重跑并重新合并。

**项目落点：** `segments.json` 定义四段，`chain_reruns.sh` 演示 A2 第 2/3/4 段的链式修复，报告记录 `regenerated_segments` 和本次成本。

### 7.5 把模型不确定性显式写成约束

**问题：** 参考图和文字描述会互相争夺控制权，密集小字、手指数量、包装形态和跨段人物一致性尤其不稳定。

**机制：** 给不同参考图划分身份保持、属性迁移或完全保持职责；每段写主体、保留关系、动作和声音，再追加具体负面约束。

A2 的三处穿帮说明，**很多"模型不稳定"其实是提示词自己写出来的**：写了"另一只手伸进来点标签"，画面就多一只手；同时写"拿出一瓶"和"箱里始终六瓶"，瓶子就塌；用 "carton" 指纸箱，就冒出纸盒奶。这类问题改写法就能消除，和模型本身画不好密集小字要分开看。

密集小字的特写，**已验证有效的做法**是这一段只绑商品图一张、设 `fully_preserved`，画面里只写一只手握着不动（`a2-replica-004` 第 3 段，人工确认通过）。另外两条路还没用上：用商品原图慢推替代生成画面（`packshot_clip.sh` 已做好，没拼进任何成片）；先用图像模型出分镜参考帧，再把原始标签贴回去（见 7.7）。

**项目落点：** `segments.json`、`seg2_v2_prompt.txt`、`seg3_v3_prompt.txt`、`packshot_clip.sh` 和项目 skill 中的 A2 失败复盘。

### 7.6 将技术完整性与内容正确性分开验收

**问题：** 文件能播放并不代表人物一致、商品标签正确、台词没念错或剧情合理。

**机制：** 机器预审检查分辨率、帧率、时长、音轨、黑帧、冻结和 SHA-256；内容质量继续保持 `awaiting_human_review`，台词可再跑 ASR 辅助定位。

**项目落点：** `out/*/report*.json` 明确写着机器预审边界，`WORKFLOW.md` S8 定义人工复核事项。

### 7.7 下一步：Codex ImageGen 出参考帧，Claude 接力渲染

**问题：** H3 每段只绑"出镜人中景图 + 白底商品图"，构图、手的数量、商品摆法全靠文字描述；H3 文生视频出的出镜人真实感也不够。

**机制：** Codex 直接使用当前会话自带的 ImageGen 生成或编辑真实感更强的虚构出镜人，再给每段出一张参考帧，把构图定死；不再建设 OpenAI Image API 或 OpenRouter 接入。Codex 负责到图片自检和写 `work/<job>/keyframes/READY.json` 为止，Claude Code 看到后接手上传、H3 提示词调整、单段重跑和验收。标签有密集小字时不让生成模型重画文字，改用原图像素或已验证的 `fully_preserved` / 慢推保底方案。

**项目落点：** `AGENTS.md`（职责与交接约定）、`scripts/wait_handoff.sh`、`work/a2_test/keyframes/seg03_prompt.txt`。`scripts/gen_keyframe.py` 仅保留作备用,不属于当前默认路径。**尚未实跑。**

## 8. 关键设计决策

| 决策 | 备选 | 当前取舍 | 风险 | 如何验证 |
| --- | --- | --- | --- | --- |
| 结构复用而非逐字复刻 | 直接替换商品名/价格 | 默认目标为 `structure`；`replica` 仅供测试 | 当前实际成片全部是 `replica`，业务目标尚未闭环 | 渲染至少 3 个结构版变体，做文本重复率和人工转化结构评审 |
| faster-whisper 替代 WhisperX | 单独安装兼容 CUDA 的 WhisperX 环境 | 复用 H3 环境的 CUDA 库，降低安装成本 | 数字和同音词会错；**背景音乐重时整段幻觉**（A2 参考片被认成"请不吝点赞订阅转发……"，靠滤到人声频段 + 分段转写 + 关键词提示才救回，拼出来的时间戳语速不可信）；无说话人对齐 | 用人工校对样本统计字错率与时间偏差，不只记录个例 |
| JSON 文件作为阶段契约 | 数据库 / 工作流引擎 | 原型期透明、易检查、易由 agent 编辑 | 缺少正式 JSON Schema、迁移和事务保证 | 为四类核心 JSON 增加 schema 校验与兼容测试 |
| 四个 5 秒段 | 长视频一次生成 / 更细分镜 | 适配 H3 连续动作和服务器内存上限 | 节拍边界不一定正好落在 5 秒，插入镜头会被牺牲 | 记录每段台词实际时长、边界听感和动作完整率 |
| 手工写 H3 提示词 | 模板自动生成 | 先积累模型失败模式再抽象 | 产能被人工提示词限制，难以真正批量 | 从现有两例提取模板，新增第三例验证跨品类泛化 |
| 机器技术预审 + 人工内容复核 | 全自动视觉 QA | 明确能力边界，避免假阳性 | 人工标准未结构化，结果不可统计 | 建立逐段检查表并记录缺陷类型、重跑次数和通过率 |
| 本地编排、远端 GPU | 全部本地 / 云 API | 复用租用 4090 与现有 H3 worker | IP、密钥、服务重启和外部仓库形成强耦合 | 记录一次冷启动恢复耗时，并将健康检查脚本化 |

## 9. 量化与验证（含待测，建议）

### 已有可核验证据

- 酸奶测试：4 段全量生成，1440×2560、24fps、20.672 秒，耗时 1509.473 秒，估算 GPU 成本 0.8386 元；机器技术预审通过，但状态仍是 `awaiting_human_review`。
- A2 全量（`a2-replica-001`）：4 段，1440×2560、20.672 秒，耗时 1510.388 秒，估算 0.8391 元；成片再转写与稿子逐字一致，无低置信度字。两次全量耗时几乎相同（1509 / 1510 秒），可以当作同机器全量重跑的基线。
- A2 局部重跑：一次重跑 2 段耗时 924.447 秒、估算 0.5136 元；另一次重跑 1 段耗时 600.358 秒、估算 0.3335 元。和上面的全量基线相比，2 段约为全量的 61%，1 段约为 40%。除了重跑的段，合并和超分每次都要对整条片子重做，所以耗时并不和段数成正比。
- 转写：A2 参考片背景音乐重（平均 -5.4dB、峰值削顶），直接转写整段幻觉，处理过程写在 skill 的 S2。
- 从 A2 全量到可交付这一轮，公钥 21:22 装好，21:28 提交正片，21:54 出片，全程 32 分钟。

### 人工验收发现的缺陷（A2）

机器预审全部通过，下面这些都是人工看片才发现的。

| 段 | 现象 | 原因 | 修法 | 状态 |
| --- | --- | --- | --- | --- |
| 2 | 开箱时瓶子突然塌下去 | 提示词同时写了"把箱子朝镜头倾斜""拿出一瓶"和"箱里始终六瓶"，互相矛盾 | 箱子全程不动；写明哪一瓶被拿出、剩下五瓶保持直立；补一句硬塑料不许塌、不许变形 | 003 重跑后修复，004 人工确认通过（2026-09-19） |
| 3 | 背标字糊成乱码、瓶型变了、没拍成特写、两只手拿奶另一只手指着 | 写了"另一只手伸进来点标签"；绑的出镜人中景图把构图拉回中景；ref2va 画不出密集小字 | 只绑背面图、一只手握着不动；保底慢推已备好 | 004 只绑背面图重跑后人工确认通过（2026-09-19），保底慢推未启用 |
| 4 | 出现原片那种纸盒奶 | 用 "carton" 指纸箱，这个词也有"纸盒奶"的意思；原片产品形态被补进画面 | 不再用 carton；写明"画面里唯一的产品是 `<Picture 2>`"，逐项禁止纸盒、利乐包 | 002 重跑后修复，004 人工确认通过（2026-09-19） |
- 两条参考视频分别为 16.47 秒和 18.67 秒，输出统一扩展为约 20.67 秒的四段视频。
- 仓库内存在两个商品参数表、两份节拍表、两份测试脚本、对应 H3 提示词和四个本地 MP4，说明原型已实跑。

### 待测与建议

1. **量产能力待测：** 至少对同一商品生成 10 个 `structure` 变体，统计两两 5-gram 重合率、失败率、人工改稿时长和最终通过率。
2. **事实一致性待测：** 抽取所有数字、成分、功效和价格表达，逐条核对 `claims[]` 和 `facts[].evidence`，统计未被证据覆盖的语义数量。
3. **ASR 质量待测：** 建立人工真值文本，分别测干净人声和重背景音乐下的字错率、低置信度召回率与时间戳误差。
4. **视频生成质量待测：** 按人物一致、商品形态、标签可读、手部、动作完成、台词准确六类缺陷打分，记录每段平均重跑次数。
5. **局部重跑收益待测：** 在相同机器、相同分镜上比较全量重跑与单段重跑的耗时、成本和最终拼接一致性。
6. **可靠性待测：** 模拟服务器重启、worker 中断、下载失败和报告缺失，验证能否幂等恢复以及是否会重复计费。
7. **产品化完成度待测：** 已有本地 Git 仓库（尚未推送到 GitHub），但还没有依赖清单、自动测试、统一 CLI 和正式 schema；建议先补最小可重复运行基线，再评估扩展为批处理服务。

## 10. 当前完成度判断

**结论：这是一个已跑通两例、经验非常具体，但仍处于“人工驱动工程原型”阶段的项目。**

已完成的是：参考片预处理、GPU 转写、人工节拍标注、带证据的商品数据、受约束的测试口播、四段 H3 生成、局部重跑、技术报告和 skill 沉淀。

尚未完成的是：真正的结构版批量生成闭环、自动文案变体、自动提示词生成、统一端到端命令、正式 schema、测试体系、推送到远端仓库、稳定的远端基础设施与结构化人工验收；图像模型出参考帧 + Codex→Claude 交接的首次实跑（见 7.7）。
